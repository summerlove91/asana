<h1>Welcome#asana</h1>
<%= form_tag(:action => 'get_project_id', :method => "get") do %>
  <%= label_tag(:project_id_input, "Enter Projet ID:") %>
  <%= text_field_tag(:project_id_input) %>
  <%= submit_tag("Search") %>
<% end %>
<% @tasks.each do |id, name| %>
    <div class="media">
        <div class="media-body">
            <h4 class="media-heading">
                <%= link_to task.name %>
            </h4>
        </div>
    </div>
<% end %>
